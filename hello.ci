mainmodule hello {
  readonly CProxy_Main mainProxy;
  readonly int nElements;
  readonly int iters;

  mainchare Main {
    entry Main(CkArgMsg*);
    entry void done();
  };

  array [1d] Hello {
    entry Hello();
    entry void SayHi(void);
  };
};
